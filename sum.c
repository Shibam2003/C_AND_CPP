#include<stdio.h>
int main()
{
	int a,n,b;
	printf("Enter the two nos.:");
	scanf("%d%d",&a,&b);
	n=a+b;
	printf("Sum of the two no: is %d",n);
	return 0;
}printf("Sorted Array: "); 
for(i = 0; i < n; i++)
printf("	%d", array[i]);
